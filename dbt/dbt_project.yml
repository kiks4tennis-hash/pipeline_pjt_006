name: "ec_pipeline"
version: "1.0"
config-version: 2

profile: "ec_pipeline"

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  ec_pipeline:

    staging:
      +materialized: view

    marts:
      +materialized: table